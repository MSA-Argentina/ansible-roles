description     "{{supervisor_service_name}}"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

exec /usr/local/bin/supervisord --nodaemon --configuration {{supervisor_config_dir}}/supervisord.conf